-- Add fine-grained source ordering to existing installations.
ALTER TABLE Sources
ADD COLUMN sub_priority INTEGER DEFAULT 0 NOT NULL;

CREATE INDEX IF NOT EXISTS idx_sources_priority_sub_priority
ON Sources(priority, sub_priority DESC, source_id ASC);

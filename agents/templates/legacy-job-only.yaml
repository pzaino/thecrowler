format_version: v1
jobs:
  - name: LegacyDiscoveryJob
    process: serial
    trigger_type: manual
    trigger_name: legacy_discovery
    steps:
      - action: RunCommand
        params:
          command: echo "legacy mode"
